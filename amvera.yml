meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  additionalCommands: npm run build
# build:
#   additionalCommands: npm run amvera:build

  run:
  command: npm run nodemon
  containerPort: 7070